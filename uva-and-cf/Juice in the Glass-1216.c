#include<stdio.h>

#define PI (2*acos(0.0))

int main()
{
    double r1,r2,h,p;
    int i, testCase;

    scanf("%d",&testCase);

    for(i=1;i<=testCase;i++)
    {
        double juiceInTheGlass, a,b;
        scanf("%lf%lf%lf%lf",&r1,&r2,&h,&p);
    }
}
