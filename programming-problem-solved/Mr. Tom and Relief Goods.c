#include<stdio.h>

int main()
{
    int testCase,i,evenType,shirtSize[5];

    scanf("%d",&testCase);

    for(i=0;i<testCase;i++)
    {
        scanf("%d%d",&evenType,&shirtSize);

        if(evenType == 1)
        {
            a++;
            shirtSize[i] = shirtSize;   //1 1 1 2 1 3
        }
    }

}
