#include<stdio.h>

int main()
{
    int n,input[3],a,b,c;

    scanf("%d%",&n);
    for(i=0;i<3;i++)
    {
        scanf("%d",&input[i]); //5,5,2
    }
    temp = input[0];
    //short a,b &c
    for(i=0;i<3;i++)
    {
        for(j=i;i<2;j++)
        {
            if(input[i]>)
        }
    }

    if((n-a)==0) printf("1\n");
    else if((n-b)==0) printf("1\n");
    else if((n-c)==0) printf("1\n");

}
